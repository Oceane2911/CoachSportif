<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: Arial, sans-serif; background: #f4f4f4; margin: 0; padding: 0; }
        .container { max-width: 600px; margin: 40px auto; background: #fff; border-radius: 12px; overflow: hidden; box-shadow: 0 4px 20px rgba(0,0,0,0.1); }
        .header { background-color: #1E1E1E; padding: 30px; text-align: center; }
        .header h1 { color: #FF4757; margin: 0; font-size: 24px; }
        .header p { color: #ccc; margin: 8px 0 0; }
        .body { padding: 30px; }
        .body p { color: #333; line-height: 1.6; }
        .info-table { width: 100%; border-collapse: collapse; margin: 20px 0; }
        .info-table tr { border-bottom: 1px solid #eee; }
        .info-table td { padding: 12px 8px; font-size: 14px; }
        .info-table td:first-child { font-weight: bold; text-transform: uppercase; color: #888; font-size: 12px; width: 40%; }
        .info-table td:last-child { color: #1E1E1E; }
        .footer { background: #f9f9f9; padding: 20px 30px; text-align: center; color: #aaa; font-size: 12px; border-top: 1px solid #eee; }
        .badge { display: inline-block; background: #FF4757; color: #fff; padding: 6px 16px; border-radius: 20px; font-size: 13px; font-weight: bold; margin-bottom: 20px; }
        .code-block { background: #f4f4f4; border-left: 4px solid #FF4757; padding: 14px 20px; margin: 20px 0; font-size: 22px; font-weight: bold; letter-spacing: 4px; text-align: center; color: #1E1E1E; border-radius: 6px; }
        .btn { display: inline-block; background: #FF4757; color: #fff; padding: 12px 28px; border-radius: 25px; text-decoration: none; font-weight: bold; font-size: 14px; margin-top: 10px; }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>Coach Sportif</h1>
            <p>Accès à votre document</p>
        </div>
        <div class="body">
            <span class="badge">📄 Document disponible</span>
            <p>Bonjour <strong>{{ prestation.prenom }} {{ prestation.nom }}</strong>,</p>
            <p>Un document vous a été transmis. Pour y accéder, rendez-vous sur notre site et saisissez votre code personnel :</p>

            <div class="code-block">{{ accessCode }}</div>

            <table class="info-table">
                <tr>
                    <td>Lien d'accès</td>
                    <td><a href="{{ absolute_url(path('app_document_access')) }}">Accéder à mon document</a></td>
                </tr>
            </table>

            <p>Ce code est strictement personnel, ne le partagez pas.</p>
            <p>À bientôt !</p>
        </div>
        <div class="footer">
            Cet email a été envoyé automatiquement, merci de ne pas y répondre.
        </div>
    </div>
</body>
</html>
